# OSとカーネル
##  カーネル Kernel(核)  ちなみに同じ発音でcolonel(大佐 カーネル サ..ス)があるが意味が違う
  複数のタスクを動かすための機構
  - プログラム実行の管理
    - タスク(プロセス、スレッド)管理
    - スケジューリング、切り替え 
      どの順番でCPUに実行させるか割り振る。
    - スレッドは昔は**LWP**(*L*ight *W*eight *P*rocess) と呼ばれていた
  - メモリの管理
    - メモリの使用、空きの管理
      カーネル、プロセス毎に管理
    - 物理メモリと仮想アドレス(論理アドレス)。itronのようなベアメタルだと
  - 割り込み
  - タイマー 
    タイムシェアリング、周期処理
  
##  OS(*O*perating *S*ystem)
  - カーネル と サービス類を合わせたもの
  - ハードウェア資源の管理
    - デバイスドライバ等
  - サービス
    - ネットワーク
    - ストレージ、ファイルシステム
    - 端末
    - サウンド
    ｰ 描画
  - ユーザランドサービス
    - デーモン類
      http,ssh,ntp,login, ...
# Linuxとは
　リーナストーバルズ(Linus B. Torvalds )が趣味で？開発したOS,
  Unix の*ような*OSを パソコンで動かしたいということから始まった。
  狭義ではカーネル（ドライバなども含む)のことだけをLinuxと呼ぶ
  サービス、コマンド、UIはUnix(オープンソース版)を使うようにしている
## Unixとは
  Dec (現在 HPに買収)のミニコンで動くOSとして作られた<br>
　この時に、UNIXを作るためにC言語が作られた カーニハン & リッチー= K&R
## UNIXという考え方
「何をしているのかわからないのなら、ここにいるべきではない」<br>
ラーメン太郎　コール　トッピングなどを聞かれる<br>
「ニンニクいれようか？」-> 「お願いします」→「はあ？」「にんにく」が正解<br>
「ヤサイカラメ」　対話型ではなく、コマンドを事前にしっていて規則で与えるだけ<br>
- small is beutiful
   小さなプログラム
      - 他のツールと組み合わせやすい
      - わかりやすい
      - 保守しやすい
- できるだけ早く試作する
  - 試作によって学ぶ
  - 速い試作はリスクを減らす
    - 試作を完全に終えて作るはリスク
- ソフトウェアを梃子(てこ)とする
   よいコードは借りてくる
## ディストリビューション　
  カーネルだけでは、何もできないので、コマンド、ライブラリ、ユーザインターフェースなどの実行環境まで含めたもの
　Redhat,Debian、SUSEなど
　ディストリビューション毎にバージョンがあり、パッケージ管理、ライブラリバージョンなどが異なる。

    
# ファイルシステム
 - Linuxはファイルシステムでできている
 - Linuxのユーザランドからの操作はファイルを通して行う

## ファイルの種類
  - 通常ファイル
  - ディレクトリ
  - リンク
  - デバイス character , block device
    - character device:  /dev/tty*XX* など、
    - block device  : /dev/sdaなどストレージ
  - パイプ   通信用 mkfifoで作る
  - ソケット

## 属性 
   - 所有者 ,グループ,その他の３つの分類でアクセス制御を行う
### ユーザとグループ
  - Linuxは複数ユーザで使用することが前提
  - ユーザはグループに所属   1つのユーザは複数のグループに所属できる
  - ユーザ /etc/passwd に設定
  - グループ /etc/groups に設定
### アクセス権
  - r:読み込み可能 w:書き込み可能(削除可能), x:実行可能(ディレクトリの場合はcd可能)
  - パーミッション アクセス権
    - 所有者 ,グループ,その他の３つの分類でアクセス制御を行う
  - chmodで設定
     - chmod +r , chmod +w, chmod +x, 
     - chmod 644   8進数で設定
  - setuid  実行時に、所有者の権限で実行される
  - スティッキービット `/tmp`などについている
    ディレクトリ内のファイルの削除は、所有者でないとできない<br>
    他ユーザによって削除されることはない。<br>
      `chmod +t dir`
    
## ディレクトリ
ｰ ディレクトリ
   ファイルの場所の管理単位　<br>
   ツリー構造の`/`(root)を頂点としたヒエラルヒー
- 特殊ディレクトリ
  - 相対ディレクトリ
    - `.` カレント
    - `..` 一つ上の親
  - 絶対PATH と相対PATH
  - `/`で始まるのは絶対  /home/user/a.out
  - `.`, `..`で始まるのは相対  `./a.out` `../a.out`

# UNIX/Linuxのコマンド
  - 英単語を短縮したようなもの
    ls = list,  cd = change directory
  - オプションは `-`, `--`
    - `-`は１文字のオプションで、複数同時に組み合わせ出来る
        -aCFl  = -a -C -F -l
    - `--`は長いオプション名 個別で指定
       --all, --inode

## コマンドはどこからどうやって入力
 - 端末   gnome-terminal , xterm, からキー入力
## シェル
  - シェルとは、人間がわかる命令を、OS,カーネルに伝える役割を持つプログラムのこと
  - B shell= **B**ourne **sh**ell)が基本
    bash (bourne again shell), dash, ksh, zshなど
  - C shell  csh  文法が若干異なる.  UCB (*U*niversity of *C*alifornia *B*erkeley)

    

## ファイル操作コマンド
  - ls  : file *l*i*s*t
    fileの情報を表示
    lsだけだと、カレントディレクトリ(.)にあるファイルリストを表示
    - -l
      ファイルの詳細　ファイルタイプ、パーミッション、所有者、サイズを表示
    - -i  iノード表示
      ファイルのIDみたいな番号
    - -F  ファイル名の後ろにファイルタイプ、パーミッションの記号が表示される
        `*` 実行可能 `/`ディレクトリ  `@`シンボリックリンク `|`パイプ
     
  - mkdir  : ディレクトリ作成 make directory
    - -p : 再帰的に作成   `mkdir -p a/b/c` とやると、a/b/cの階層ができる。
  - rmdir  : ディレクトリ削除  remove directory
    空ディレクトリであれば削除
  - rm :ファイル削除 remove
    - -f 強制的に
    - -i 削除してよいか問い合わせてくる
    - -r ディレクトリを再帰的(recursively) に削除する
  - mv  : ファイルの移動、名前の変更  move
  - ln  : ファイル、ディレクトリのリンク
    - -s シンボリックリンク
  - chmod  : ファイルのパーミッションを変更 change mode
    `chmod +r file` 読み込み可能
    `chmod +w file` 書き込み可能
    `chmod +x file` 実行可能
    `chmod 644 file` 所有者は読み書き可能、その他は、読み込みのみ
    `chmod +t dir` スティッキービット設定
  - chown : 所有者変更  change owner
     `chown kawamoto:users file` 所有者をkawamotoにする
  - pwd :  現在のカレントディレクトリを表示
  - cd : 現在のカレントディレクトリを変更  chage current directory
  - cat ファイルの内容表示   con*cat*inate コンカチ 結合
  - wc  : ファイルの中の、行数、単語数、文字数を表示  word count
  - mount マウント
    ブロックデバイス(ストレージ)をファイルシステムのどこかに配置してアクセスできるようにする。
  - umount
    マウントの解除
  - findmnt マウントの状態を表示
    - touch 空のファイルを作成する
          ファイルのタイムススタンプを変更
    ```
    $ touch -d "2026-07-01 10:12:13" lesson2.md
    ```

    - cd   現在のディレクトリ(カレントディレクトリ）の移動
      引数無し　　ホームディレクトリに移動
    ```bash
      cd /home/user    <- ユーザ名=userの場合
      cd ~    チルダ
      cd $HOME
    ```
- alias コマンドの別名定義
  長いコマンドや、引数まで含めて別名のコマンド名にできる
  ```bash
  alias ll='ls -lF'
  alias ll='ls -CF --color'
  ``
# 標準入出力とリダイレクト
- 標準入力 (キー入力)0
- 標準出力 (画面表示) 1
- 標準エラー 2
# リダイレクト
- `>` 標準出力、標準エラーをファイルに保存
`a.out > file`, `a.out 2>file`
- `<` 標準入力をプログラムの入力にする
`cat < file`
 - `>>` ファイルへの追加
 - `<<` 入力の終わりを文字列で示す エディタを使わずにファイルを作ったりできる
```bash
cat <<_EOF >file.txt
abc
def
ghk
_EOF
```

# コマンド入力
シェルからはキー入力
コマンドと引数を同じ行に入力。区切りは空白。
## カーソル移動,編集
ctrl+B  1文字後退
ctrl+F  1文字前進
ctrl+A  行の先頭へ
ctrl+E  行の先頭へ
ctrl+U  現在行の入力を全部消し、カーソルを一番左に移動
ctrl+K  カーソル位置より行末(右端)まで削除
ctrl+W  カーソル位置より行頭(左端)まで削除

## コマンドの履歴
過去に入力したコマンドはバッファに覚えている
- ctrl+P (previous)現在より昔の方に履歴をたどる
- ctrl+N (next)現在より次の方に履歴をたどる
- ctrl+R + コマンドの一部入力   一部入力した文字を含むコマンドを昔のほうに探す
- ctrl+S ctrl+Rでさかのぼったあと、現在の方に向かって探す(ctrl+Rの逆方向)
例
```bash
ls -l test.txt
!!
gcc a.c b.c
!g
echo $*
cat !$
!g
cat !:1
```
`!!`：直前のコマンドの再実行
`!g`: ヒストリをさかのぼってgで始まる行を探して実行  
`!*` 直前のコマンドの全引数
`!$` 直前のコマンドの最後(右端)の引数
`!:1` 直前のコマンド引数の１番目   :2は２番目...


